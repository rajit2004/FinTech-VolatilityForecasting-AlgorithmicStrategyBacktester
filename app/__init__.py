"""The app package holds the whole backend of the capstone project.

It is split into small sub packages so that data handling, feature
engineering, models, strategies, backtesting and the API stay separate
from each other. This keeps the code easy to read and test.
"""

__version__ = "1.0.0"
